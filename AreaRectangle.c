//Tut2P3: Program to calculate and print area of a rectangle
#include <stdio.h>
void main()
{
    int l, b;
    printf("Enter the length of rectangle: ");
    scanf("%d", &l);
    printf("Enter the breadth of rectangle: ");
    scanf("%d", &b);
    printf("Area of circle is %d",l*b);
}
